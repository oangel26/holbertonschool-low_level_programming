#include "main.h"

/**
 * _atoi - writes the character c to stdout
 * @s: The string
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int _atoi(char *s)
{
	return (0);
}
